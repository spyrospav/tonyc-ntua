<*
 * Test case: simple-list.tony
 * Result: Pass
 *>

 def program():
  list[int] a
  list[int] b
  putc('\n')
  a := nil
  putb(nil?(a))
  a := 5 # a
  putb(nil?(a))
  puti(head (a))
  putc('\n')
  a := 6 # a
  puti(head (a))
  putc('\n')
  a := 8 # (7 # a)
  putc('\n')
  puti(head (tail (a)))
  putc('\n')
  b := tail (a)
  puti(head (tail (tail (b))))
  putc('\n')
end
